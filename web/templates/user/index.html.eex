<h1> User index </h1>

<table class="table">
  <%= for user <- @users do %>
    <tr>
      <td><b><%= user.name %></b></td>
      <td>
      <%= if user.status == nil do
            "Inactive"
          else
            user.status
          end %>
      </td>
      <td><%= link "View", to: user_path(@conn, :show, user.id) %>
    </tr>
  <% end %>
</table>
